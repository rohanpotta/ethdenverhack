# 0G Agent Shield Configuration
# ================================

# Your wallet private key (WITHOUT 0x prefix)
# This key is used for 0G Storage transactions AND as the seed for vault encryption.
# The agent never sees this key — it only accesses decrypted data in-memory.
PRIVATE_KEY=your_private_key_here

# 0G Galileo Testnet (Chain ID: 16602)
# These are the official endpoints from https://docs.0g.ai/developer-hub/testnet/testnet-overview
# and https://github.com/0gfoundation/0g-storage-ts-starter-kit
EVM_RPC=https://evmrpc-testnet.0g.ai
INDEXER_RPC=https://indexer-storage-testnet-turbo.0g.ai

# 0G Mainnet (uncomment for production)
# EVM_RPC=https://evmrpc.0g.ai
# INDEXER_RPC=https://indexer-storage.0g.ai

# Optional: Override vault encryption passphrase
# If not set, derived from PRIVATE_KEY (recommended)
# VAULT_SECRET=your_custom_passphrase

# Optional: protect /api/push-event when dashboard/MCP traffic crosses ngrok/public internet.
# If set, include the same value in MCP env as PUSH_EVENT_TOKEN.
# PUSH_EVENT_TOKEN=choose-a-long-random-token
# DASHBOARD_API=http://localhost:3000

# Optional: 0G Compute inference endpoint for DeFAI rationale generation.
# If not set, SILO uses deterministic local heuristics (safe fallback).
# OG_COMPUTE_URL=https://your-0g-compute-endpoint
# OG_COMPUTE_MODEL=qwen-2.5-7b-instruct
# OG_COMPUTE_API_KEY=optional_api_key_if_required
# OG_COMPUTE_PROVIDER_ADDRESS=0x... # required when using starter-kit /api/services/query
# OG_COMPUTE_FALLBACK_FEE=0.01

# Testnet contract addresses (from official docs — for reference only)
# Flow: 0x22E03a6A89B950F1c82ec5e74F8eCa321a105296
# Mine: 0x00A9E9604b0538e06b268Fb297Df333337f9593b

# Get testnet tokens: https://faucet.0g.ai (0.1 0G per day)
# Block explorer: https://chainscan-galileo.0g.ai
# Storage explorer: https://storagescan-galileo.0g.ai
